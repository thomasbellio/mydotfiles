[core]
	editor = nvim
[user]
	email = git@thomasbell.io
	name = Thomas Bell
[init]
	defaultBranch = master
[filter "lfs"]
	process = git-lfs filter-process
	required = true
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
